# === PRODUCTION DEPENDENCIES ===
# Core API framework
fastapi==0.115.6  # Replaces Flask - async, better performance, built-in validation
uvicorn[standard]==0.34.0  # Replaces gunicorn - async ASGI server

# LLM and RAG essentials  
langchain==0.3.13  # Updated to latest stable
langchain-openai==0.2.14  # Updated for security fixes
weaviate-client==4.10.2  # Latest stable with security patches
openai==1.59.2  # Updated with security fixes

# Data validation (FastAPI uses pydantic natively)
pydantic==2.10.5  # Updated for security
pydantic-settings==2.7.1  # Configuration management

# Document processing - minimal set
pypdf==5.2.0  # Updated, handles most PDF needs
PyYAML==6.0.2  # Updated with security patches

# Observability
prometheus-client==0.21.1  # Updated

# Security
cryptography==44.0.0  # Critical security updates
python-jose[cryptography]==3.3.0  # JWT handling

# === DEVELOPMENT DEPENDENCIES (requirements-dev.txt) ===
# Move these to separate requirements-dev.txt:
# pytest==8.3.4
# pytest-asyncio==0.25.2  
# ruff==0.9.2  # Replaces black+flake8, faster
# mypy==1.14.2  # Type checking