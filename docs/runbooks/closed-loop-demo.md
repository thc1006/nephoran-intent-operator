# Runbook: Closed-loop demo (MVP)
Goal: Demonstrate Observe → Analyze → Act as PR.

## Steps (placeholder)
1) Start intentd API
2) Create intent: deploy baseline + trafficgen
3) Merge PR to apply baseline
4) Generate metrics snapshot (stub)
5) Trigger analyzer threshold (stub)
6) Create a scale intent (Act) that proposes a PR (replicas++)
7) Merge PR; verify metrics improved (stub)

This runbook will be expanded after cluster tooling is wired.
