# Base component packages
This folder is a placeholder for reusable base packages (kpt/KRM).

Each component package should include:
- clear README with assumptions and upstream references
- deterministic resource naming and required labels
- safe defaults (no cluster-scoped resources by default)
