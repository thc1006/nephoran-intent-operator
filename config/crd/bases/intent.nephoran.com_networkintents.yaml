apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: networkintents.intent.nephoran.com
spec:
  group: intent.nephoran.com
  names:
    categories:
    - nephio
    - o-ran
    kind: NetworkIntent
    listKind: NetworkIntentList
    plural: networkintents
    shortNames:
    - ni
    singular: networkintent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkIntent represents a high-level network scaling and configuration
          intent
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkIntentSpec defines the desired state of a network
              intent
            properties:
              intentType:
                description: IntentType specifies the type of intent (e.g., "scaling",
                  "optimization")
                type: string
              namespace:
                description: Namespace specifies the target namespace
                type: string
              networkParameters:
                description: NetworkParameters define network-level configurations
                properties:
                  networkSliceId:
                    description: NetworkSliceID defines the specific network slice
                    type: string
                  qosProfile:
                    description: QoSProfile defines Quality of Service settings
                    properties:
                      maximumDataRate:
                        description: MaximumDataRate defines the maximum data transmission
                          rate
                        type: string
                      priority:
                        description: Priority of the network slice
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                type: object
              replicas:
                description: Replicas specifies the desired number of replicas
                format: int32
                minimum: 0
                type: integer
              scalingParameters:
                description: ScalingParameters define how network functions should
                  be scaled
                properties:
                  autoscalingPolicy:
                    description: AutoscalingPolicy defines how automatic scaling should
                      occur
                    properties:
                      maxReplicas:
                        description: MaxReplicas is the upper limit of replicas
                        format: int32
                        minimum: 1
                        type: integer
                      metricThresholds:
                        description: MetricThresholds define scaling triggers
                        items:
                          description: MetricThreshold defines a condition for scaling
                          properties:
                            type:
                              description: Type of metric (CPU, Memory, Custom)
                              type: string
                            value:
                              description: Value at which scaling occurs
                              format: int64
                              type: integer
                          required:
                          - type
                          - value
                          type: object
                        type: array
                      minReplicas:
                        description: MinReplicas is the lower limit of replicas
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                  replicas:
                    description: Replicas defines the desired number of replicas for
                      network functions
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              source:
                description: Source of the intent (e.g., "user", "automation")
                type: string
              target:
                description: Target specifies the target component or resource
                type: string
            type: object
          status:
            description: NetworkIntentStatus defines the observed state of a network
              intent
            properties:
              conditions:
                description: Conditions represent the current conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              llmResponse:
                description: LLMResponse contains the LLM processing response
                type: object
                x-kubernetes-preserve-unknown-fields: true
              message:
                description: Message contains human-readable information about the
                  status
                type: string
              observedReplicas:
                description: ObservedReplicas reflects the current number of replicas
                format: int32
                type: integer
              phase:
                description: Phase indicates the current phase of the intent
                type: string
              readyReplicas:
                description: ReadyReplicas represents the number of ready instances
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status:
        type: object
        x-kubernetes-preserve-unknown-fields: true
