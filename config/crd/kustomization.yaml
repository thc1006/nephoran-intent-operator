apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

# Markers for controller-gen CRD generation
patchesStrategicMerge:
- patches/webhook_patch.yaml

resources:
- bases
- patches