apiVersion: v1
kind: Service
metadata:
  name: webhook-manager
  namespace: nephoran-system
  labels:
    app: webhook-manager
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: webhook-server
  selector:
    app: webhook-manager
    control-plane: webhook-manager