apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: nephoran-system
spec:
  ports:
  - name: webhook
    port: 443
    targetPort: 9443
    protocol: TCP
  selector:
    app: webhook-manager