apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- serviceaccount.yaml
- deployment.yaml
- manifests.yaml
- service.yaml

# Image configuration for webhook
images:
- name: webhook-manager
  newName: ghcr.io/nephio-project/nephoran-webhook
  newTag: v0.1.0