apiVersion: kpt.dev/v1alpha1
kind: ResourceGroup
metadata:
  name: nephio-packages
  namespace: default
  labels:
    kpt.dev/inventory-id: nephio-install
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  resources: []