name: Debug GHCR Authentication - DISABLED
# DISABLED: This was a debug workflow and should not run automatically

# DISABLED - No triggers to prevent conflicts
on:
  # DISABLED: This workflow was for debugging only
  workflow_dispatch:
    inputs:
      disabled_notice:
        description: 'This workflow is DISABLED - debug completed'
        required: false
        default: 'DISABLED - Debug workflow no longer needed'

permissions:
  contents: read
  packages: write
  actions: read

env:
  GO_VERSION: "1.25.0"  # Updated to latest stable version

jobs:
  debug-auth:
    name: Debug GHCR Authentication
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      # [Rest of the workflow remains the same]
      
      # Ensure Go module caching
      - name: Go Module Caching
        run: |
          go mod download
          go mod verify