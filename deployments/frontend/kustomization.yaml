apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nephoran-system

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: nephoran-ui
  component: frontend
