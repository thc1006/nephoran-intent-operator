apiVersion: v1
kind: ConfigMap
metadata:
  name: nephoran-ui-html
  namespace: nephoran-system
  labels:
    app: nephoran-ui
    component: frontend
data:
  index.html: |
    # This file is managed by kustomize
    # The actual content is sourced from nephoran-ui.html
    # Run: kubectl create configmap nephoran-ui-html --from-file=index.html=nephoran-ui.html -n nephoran-system --dry-run=client -o yaml | kubectl apply -f -
