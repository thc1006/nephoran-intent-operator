# golangci-lint v2.x configuration
version: "2"

run:
  timeout: 5m
  issues-exit-code: 0

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused