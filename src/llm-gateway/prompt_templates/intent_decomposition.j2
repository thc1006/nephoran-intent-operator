Decompose this intent into an action plan:

"{{ intent_text }}"

Components: odu, ocu-cp, ocu-up, amf, smf, upf, nrf, nssf
Slices: embb, urllc, mmtc
Actions: create_cnf, scale_out, scale_in, update_config, delete_cnf

Output ONLY valid JSON conforming to the intent-plan schema.
