# === DOCUMENTATION BUILD REQUIREMENTS ===
# Minimal set for documentation generation

# Core documentation engine
mkdocs==1.6.1  # Pinned version for reproducibility
mkdocs-material==9.6.0  # Material theme with all essentials

# Essential plugins only (reduced from 10 to 3)
mkdocs-mermaid2-plugin==1.1.1  # Diagram support
mkdocs-git-revision-date-localized-plugin==1.3.0  # Git integration
mkdocs-minify-plugin==0.8.0  # HTML optimization

# Core extensions (bundled with mkdocs-material)
# pymdown-extensions is included with mkdocs-material
# No need for separate installation

# Minimal additional dependencies
PyYAML==6.0.2  # Config parsing (shares with main requirements)
jinja2==3.1.5  # Templating (security updates)

# Note: Removed unnecessary plugins:
# - mkdocs-swagger-ui-tag (use built-in API docs)
# - mkdocs-awesome-pages-plugin (use nav configuration)
# - mkdocs-macros-plugin (use jinja2 directly)
# - mkdocs-git-authors-plugin (unnecessary)
# - mkdocs-glightbox (use material's lightbox)
# - mkdocs-redirects (handle at web server level)
# - beautifulsoup4, lxml (not needed for basic docs)