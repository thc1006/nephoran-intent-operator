apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: rag-service
  labels:
    app.kubernetes.io/name: nephoran-rag
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nephoran-rag
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
